enum HomeWallpaperStates{
 initialState,
 loadingState,
 loadedState,
 errorState
}

enum HomeOnRefreshStates{
 initialState,
 successState,
 errorState
}

enum HomeOnLoadStates{
 initialState,
 successState,
 errorState
}